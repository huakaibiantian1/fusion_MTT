# 星形交叉场景训练说明

## 概述

新增了**星形交叉场景**生成功能，可以生成多条轨迹从中心点辐射出去的复杂交叉场景，类似于图片中显示的模式。这比简单的两条轨迹交叉更加复杂，更适合训练鲁棒的多目标跟踪模型。

## 场景类型对比

### 1. 简单交叉场景（原有）
- 2条轨迹相互交叉
- 交叉点在中心区域
- 适合基础训练

### 2. 星形交叉场景（新增）✨
- 4-8条轨迹从中心点辐射出去
- 形成星形或放射状模式
- 多个轨迹在中心区域接近或交叉
- 更复杂、更具挑战性

## 配置参数

### 1. 交叉场景概率 (`crossing_probability`)
控制有多少比例的场景包含交叉（简单或星形）

- `0.0`: 无交叉场景（等同于原始数据）
- `0.3`: 30%包含交叉（保守）
- `0.5`: 50%包含交叉（推荐）
- `1.0`: 100%包含交叉（用于专门训练）

### 2. 星形交叉概率 (`star_crossing_probability`)
在交叉场景中，星形交叉的比例

- `0.0`: 全部为简单交叉（2条轨迹）
- `0.5`: 一半星形、一半简单（平衡）
- `0.7`: 70%星形交叉（推荐）
- `1.0`: 100%星形交叉（最复杂）

### 3. 星形轨迹数量 (`num_star_trajectories`)
星形交叉场景中的轨迹数量范围

- `[4, 8]`: 4到8条轨迹（推荐，适中复杂度）
- `[6, 10]`: 6到10条轨迹（高复杂度）
- `[3, 6]`: 3到6条轨迹（低复杂度）

## 使用方法

### 方法1：使用配置文件（推荐）

已创建配置文件：`config_star_crossing.json`

```json
{
    "data": {
        "crossing_probability": 0.5,
        "star_crossing_probability": 0.7,
        "num_star_trajectories": [4, 8]
    }
}
```

使用批处理文件训练：

**Windows:**
```bash
train_star_crossing.bat
```

**Linux/Mac:**
```bash
bash train_star_crossing.sh
```

### 方法2：使用命令行参数

```bash
python train.py \
    --config config.json \
    --use-crossing \
    --crossing-prob 0.5 \
    --star-crossing-prob 0.7 \
    --num-star-trajectories-min 4 \
    --num-star-trajectories-max 8
```

### 方法3：测试和可视化

在训练之前，可以先可视化星形交叉场景：

```bash
python test_star_crossing.py
```

这将生成以下文件：
- `star_crossing_scenario_1.png` - 星形交叉场景示例1
- `star_crossing_scenario_2.png` - 星形交叉场景示例2
- `star_crossing_scenario_3.png` - 星形交叉场景示例3
- `crossing_types_comparison.png` - 简单交叉vs星形交叉对比

## 推荐配置方案

### 方案1：渐进式训练（推荐）

**阶段1：基础训练**
```json
{
    "crossing_probability": 0.3,
    "star_crossing_probability": 0.3,
    "num_star_trajectories": [4, 6]
}
```
- 适合模型初始训练
- 逐步引入复杂场景

**阶段2：增强训练**
```json
{
    "crossing_probability": 0.5,
    "star_crossing_probability": 0.5,
    "num_star_trajectories": [4, 8]
}
```
- 平衡简单和复杂场景
- 提升模型鲁棒性

**阶段3：挑战训练**
```json
{
    "crossing_probability": 0.7,
    "star_crossing_probability": 0.7,
    "num_star_trajectories": [6, 10]
}
```
- 大量复杂场景
- 最大化模型性能

### 方案2：均衡训练（当前配置）

```json
{
    "crossing_probability": 0.5,
    "star_crossing_probability": 0.7,
    "num_star_trajectories": [4, 8]
}
```

这意味着：
- 50%的场景包含交叉
- 其中70%为星形交叉（即总体35%星形）
- 星形场景有4-8条轨迹

### 方案3：专项训练

**专注星形交叉：**
```json
{
    "crossing_probability": 1.0,
    "star_crossing_probability": 1.0,
    "num_star_trajectories": [6, 10]
}
```
- 100%星形交叉场景
- 用于专门优化复杂场景性能

## 实现细节

### 星形交叉生成算法

1. **中心点选择**：在视野中心区域随机选择交叉点
2. **角度分布**：轨迹均匀分布在360度范围内
3. **速度设置**：每条轨迹随机速度
4. **时间同步**：所有轨迹在时间中点经过交叉点
5. **视野约束**：确保轨迹起点和路径在视野内

### 代码结构

```
data_generation_with_crossing.py
├── MTTDataGeneratorWithCrossing
│   ├── _generate_crossing_scenario()     # 决定生成简单或星形交叉
│   ├── _generate_crossing_pair()         # 生成简单交叉（2条）
│   └── _generate_star_crossing()         # 生成星形交叉（多条）✨新增
└── MTTDatasetWithCrossing
    └── __init__()                         # 支持星形参数
```

## 预期效果

使用星形交叉场景训练后，模型应该能够：

1. ✅ 更好地处理多目标同时接近的情况
2. ✅ 在复杂交叉场景中保持ID一致性
3. ✅ 减少在密集区域的目标丢失
4. ✅ 提升整体跟踪鲁棒性

## 注意事项

1. **计算资源**：星形交叉场景目标数量更多，训练时间可能增加
2. **内存占用**：建议 `max_targets >= 10` 以容纳更多轨迹
3. **批次大小**：如果内存不足，可以减小 `batch_size`
4. **训练步数**：建议增加训练步数以充分学习复杂场景

## 评估和对比

训练完成后，建议使用不同难度的测试集评估：

```python
# 评估简单交叉
test_loader_simple = create_dataloaders_with_crossing(
    crossing_probability=1.0,
    star_crossing_probability=0.0  # 只有简单交叉
)

# 评估星形交叉
test_loader_star = create_dataloaders_with_crossing(
    crossing_probability=1.0,
    star_crossing_probability=1.0  # 只有星形交叉
)
```

## 示例输出

运行测试脚本后的输出：

```
========================================
测试星形交叉场景生成
========================================

生成场景 1/3...
  轨迹数量: 7
  帧数: 50
  轨迹间最小距离: 2.34m (平均: 15.67m)
  已保存: star_crossing_scenario_1.png

生成场景 2/3...
  轨迹数量: 6
  帧数: 50
  轨迹间最小距离: 1.89m (平均: 18.23m)
  已保存: star_crossing_scenario_2.png

...
```

## 常见问题

**Q: 星形交叉场景会让模型训练变慢吗？**

A: 会略微增加训练时间，因为每个场景的目标数量更多，但提升的性能值得这个代价。

**Q: 我应该只使用星形交叉训练吗？**

A: 不推荐。建议混合使用简单场景、简单交叉和星形交叉，让模型学习多样化的场景。

**Q: 如何确定最佳的轨迹数量？**

A: 建议从 `[4, 8]` 开始，根据应用场景调整。如果实际应用中经常出现10个以上目标同时接近，可以增加到 `[6, 10]`。

**Q: 星形交叉和真实场景的关系？**

A: 星形交叉模拟了多个目标在同一区域汇聚的情况，这在十字路口、广场等场景中很常见。

## 总结

星形交叉场景是对原有简单交叉的重要扩展，能够显著提升模型在复杂多目标场景中的表现。建议按照渐进式训练方案，从简单到复杂逐步提升模型能力。
