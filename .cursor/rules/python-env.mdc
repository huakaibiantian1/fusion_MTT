---
description: 指定本项目使用 minimind conda 环境运行所有 Python 命令
alwaysApply: true
---

# Python 环境规则

本项目所有 Python 命令必须使用 minimind conda 环境的解释器。

## 固定解释器路径

```
C:\Users\yy\.conda\envs\minimind\python.exe
```

## 规则

- 运行任何 `.py` 脚本时，始终使用上述完整路径调用 Python，不使用裸 `python` 命令。
- 安装包时使用：`C:\Users\yy\.conda\envs\minimind\python.exe -m pip install <包名>`
- 可用包已确认：torch 2.6.0+cu126、scipy 1.15.2、numpy 2.1.2

## 示例

```powershell
# ✅ 正确
C:\Users\yy\.conda\envs\minimind\python.exe train.py --use-multi

# ❌ 错误
python train.py
```
