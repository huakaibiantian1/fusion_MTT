# 基于 Transformer 的多目标跟踪系统航迹起始与终结算法设计

## 摘要

本文详细描述了在 BAIT（Bayesian Inference using Transformers for Multi-Target Tracking）框架中引入航迹起始（Track Initiation）与航迹终结（Track Termination）能力的算法设计方案。该方案通过构造逐槽位存在性建模机制、设计面向动态目标数的训练数据集、以及引入专项存在性损失函数，使模型具备从纯量测序列中自主检测目标出现与消亡的能力，无需依赖外部真值辅助初始化。

---

## 1. 问题描述

### 1.1 原始框架的局限性

在原始 BAIT 框架中，模型的推理前提依赖以下假设：

1. **静态目标数假设**：场景中所有目标从第 $0$ 帧开始同时存在，直至最后一帧同时终结，目标数在全程保持不变。
2. **真值热启动假设**：在模型开始推理的前 $\tau$ 帧，将目标的真实位置直接注入历史状态矩阵 $\mathbf{S}_\text{past}$，相当于为模型提供了外部的先验知识。
3. **前缀连续性假设**：内部固定槽位的 GT 标签被设计为"前 $N$ 个槽位有效，其余无效"的前缀形式，无法表示任意槽位的激活/失活状态。

上述假设在工程实际中均不成立：目标会在任意时刻进入观测视野，也会在任意时刻离开或被遮挡消失；跟踪系统必须具备对此类动态变化进行自主判断的能力。

### 1.2 目标能力定义

本方案的设计目标是使系统满足以下能力约束：

- **自主起始**：仅依据历史量测序列，在目标首次产生有效量测时建立对应轨迹槽，无需任何真值辅助。
- **自主终结**：在目标停止产生量测（漏检积累或真实消亡）时，预测对应槽位的存在概率下降至阈值以下，并停止对该槽位的有效输出。
- **生命周期交错处理**：在多目标场景下，各目标的生命周期相互独立，模型须同时维护处于不同阶段（起始期、稳定期、终结期）的多个航迹。

---

## 2. 系统架构

### 2.1 整体数据流

```
原始量测序列 Z_{0:T}
       │
       ▼
  [量测嵌入层]  ──────────────────────────┐
       │                                  │ Cross-Bridge
  [Track Encoder]                   [Meas Encoder]
       │                                  │
       └────────── 交叉注意力增强 ──────────┘
                        │
               [Associate Decoder]
                        │
              匹配概率矩阵 MPM [B, M, T+1]
                        │
                  Sinkhorn 软赋值
                        │
              [Filtering Decoder]
                    /         \
         filtered_states    existence_probs
         [B, T, 3]           [B, T]
              │                   │
         位置滤波输出         存在性判决
         （活跃槽有效）        （阈值过滤）
```

### 2.2 固定槽位机制（Fixed-Slot Assignment）

系统维护 $T_\text{max}$ 个固定轨迹槽，每个槽 $i$ 在整个推理过程中始终对应同一物理目标 $\tau_i$。

**槽位状态定义**：

$$
\text{slot}_i(t) = \begin{cases}
\mathbf{x}_i(t) & \text{若 } t \in [b_i, d_i] \text{（目标存活）} \\
\mathbf{0} & \text{若 } t \notin [b_i, d_i] \text{（目标未激活）}
\end{cases}
$$

其中 $b_i, d_i$ 分别为目标 $i$ 的起始帧和终结帧，$\mathbf{x}_i(t) \in \mathbb{R}^3$ 为归一化后的三维位置坐标。

该设计保证了槽位在时间轴上的语义一致性：对同一轨迹槽位在任意帧的特征均可被 Track Encoder 的 Attention 机制跨帧关联，不受生死事件影响。

---

## 3. 存在性建模

### 3.1 存在性概率头（Existence Probability Head）

在 Filtering Decoder 的输出之上，增设独立的存在性预测头：

$$
e_i = \sigma\left(\mathbf{W}_e \cdot \mathbf{h}_i^{\text{filt}} + b_e\right) \in (0, 1)
$$

其中 $\mathbf{h}_i^{\text{filt}}$ 为第 $i$ 个槽位在 Filtering Decoder 最终层的隐状态，$\sigma(\cdot)$ 为 Sigmoid 函数，$e_i$ 为该槽位目标在当前帧的预测存在概率。

与直接对匹配概率矩阵做最大值判断相比，该设计的优势在于：
- 存在性判断由完整的历史轨迹特征（含跨帧注意力）驱动，而非单帧量测的局部证据；
- 即使在漏检（missed detection）帧，模型仍可通过历史运动状态维持合理的存在性估计；
- 目标消亡后，由于历史槽位的零填充累积，模型逐步感知到该槽位缺乏有效更新，$e_i$ 自然趋近于 0。

### 3.2 GT 存在性掩码（Existence Ground-Truth Mask）

对于批次中每个样本，逐槽位构造二值 GT 掩码：

$$
\xi_i = \begin{cases}
1 & \text{若 } b_i \leq f_\text{cur} \leq d_i \\
0 & \text{otherwise}
\end{cases}, \quad i = 1, \ldots, T_\text{max}
$$

向量 $\boldsymbol{\xi} = [\xi_1, \xi_2, \ldots, \xi_{T_\text{max}}]^\top \in \{0,1\}^{T_\text{max}}$ 支持任意槽位组合的激活状态，彻底突破了原有"前缀连续性"假设，可精确表达生命周期交错的多目标场景。

### 3.3 存在性损失函数

采用逐槽位二值交叉熵（BCE）损失：

$$
\mathcal{L}_\text{exist} = -\frac{1}{T_\text{max}} \sum_{i=1}^{T_\text{max}} \left[ \xi_i \log e_i + (1 - \xi_i) \log (1 - e_i) \right]
$$

该损失函数对所有槽位平等施加监督，无论其当前处于激活状态还是静默状态，均参与梯度反传，从而使模型同时学习"何时激活槽位"和"何时关闭槽位"两种行为。

---

## 4. 滤波损失的存在性感知掩码

### 4.1 原始滤波损失的问题

原始框架的滤波损失形式为：

$$
\mathcal{L}_\text{filt}^{\text{old}} = \frac{1}{N} \sum_{i=1}^{N} \left\| \hat{\mathbf{x}}_i - \mathbf{x}_i^* \right\|_2^2
$$

其中 $N$ 为当前帧活跃目标总数，只对"前 $N$ 个连续槽位"计算损失。该设计在生命周期交错场景下产生语义错误：若第 0 号目标已死亡而第 2 号目标仍存活，"前 $N$ 个槽位"的约定导致损失掩码与真实激活状态不符。

### 4.2 改进后的存在性感知滤波损失

将滤波损失的计算掩码由"前缀整数截断"改为 GT 存在性掩码：

$$
\mathcal{L}_\text{filt} = \frac{\sum_{i=1}^{T_\text{max}} \xi_i \cdot \left\| (\hat{\mathbf{x}}_i - \mathbf{x}_i^*) \cdot C \right\|_2^2}{\sum_{i=1}^{T_\text{max}} \xi_i}
$$

其中 $C = 50000\,\text{m}$ 为坐标归一化常数，分母的 $\sum \xi_i$ 为当前帧实际存活目标数，分子仅对存活槽位计算物理空间的均方误差。

这一改动确保：对已终结目标槽位的零填充不引入虚假梯度；对尚未起始的目标槽位同样不施加位置监督。

---

## 5. 总损失函数

$$
\mathcal{L}_\text{total} = \lambda_\text{assoc} \cdot \mathcal{L}_\text{assoc} + \lambda_\text{filt} \cdot \mathcal{L}_\text{filt} + \lambda_\text{exist} \cdot \mathcal{L}_\text{exist}
$$

| 分项 | 默认权重 | 作用 |
|------|---------|------|
| $\mathcal{L}_\text{assoc}$（CE + Dice） | 1.0 | 量测-轨迹关联正确率 |
| $\mathcal{L}_\text{filt}$（存在性感知 MSE） | 1.0 | 活跃槽位的位置精度 |
| $\mathcal{L}_\text{exist}$（逐槽位 BCE） | 1.0 | 航迹起始/终结时刻判断 |

三项损失联合训练，使模型在同一前向传播中同时优化关联能力、状态估计精度和生命周期感知能力。

---

## 6. 测量热启动的去真值化

### 6.1 原始热启动机制的缺陷

原始框架在推理的前 $\tau$ 帧直接将目标真实位置注入历史状态矩阵：

$$
\mathbf{S}_\text{past}[i, t] = \mathbf{x}_i^*(t), \quad t \in [0, \tau)
$$

这一操作本质上是一种**测试时数据泄露**，使模型在推理早期获得了训练时不应假设存在的外部信息。真实部署场景中，系统无法获知目标的初始精确位置。

### 6.2 纯量测热启动

改进后，历史状态矩阵的初始化严格仅依赖量测：

$$
\mathbf{S}_\text{past}[i, t] = \begin{cases}
\mathbf{z}_{i}(t) & \text{若帧 } t \text{ 存在目标 } i \text{ 的关联量测} \\
\mathbf{0} & \text{否则（漏检或目标尚未起始）}
\end{cases}
$$

其中 $\mathbf{z}_i(t)$ 为经过归一化的含噪量测坐标，包含传感器观测误差但不包含真实位置信息。

该改动具有以下效果：
- **训练/推理一致性**：训练时的量测噪声、漏检等不确定性在推理时同样存在，消除了域偏移；
- **对漏检的鲁棒性**：对于 $P_d < 1$ 场景，即使某帧目标量测缺失，系统仍能以全零向量维持槽位结构，不会产生数值异常；
- **航迹起始的自然实现**：目标在 $b_i$ 帧之前，对应槽位的历史状态均为全零，量测对该槽位的贡献为零；在 $b_i$ 帧之后，有效量测逐步填充该槽位，存在性概率自然从低值上升，实现软性的航迹起始判决。

---

## 7. 生命周期交错训练场景

### 7.1 场景设计动机

为使模型充分学习航迹起始与终结的判断能力，设计了专项训练场景 **`birth_death`**。该场景聚焦于以下挑战：

- 场景中同时存在处于不同生命周期阶段的多条目标；
- 某些帧仅有部分目标的量测可见，系统须区分"目标尚未出现"、"目标漏检"与"目标已终结"三种状态；
- 目标数量在时间轴上动态变化，对关联矩阵和存在性判决均产生压力。

### 7.2 场景生成规则

每条目标轨迹的生命周期参数独立采样：

| 参数 | 采样范围 | 说明 |
|------|---------|------|
| 目标总数 $N$ | $\text{Uniform}\{3, 8\}$ | 场景复杂度可变 |
| 起始帧 $b_i$ | $\text{Uniform}\{0, \lfloor T/3 \rfloor\}$ | 目标进入视野时刻随机 |
| 终结帧 $d_i$ | $\text{Uniform}\{\lceil 2T/3 \rceil, T-1\}$ | 目标离开视野时刻随机 |
| 最短存活时长 | $\geq \lfloor T/3 \rfloor$ 帧 | 保证有效量测序列长度充足 |

目标运动模型采用三段恒加速度（Constant Acceleration, CA）模型，各段加速度独立采样，具有较强的机动性。量测模型与其他场景一致，包含球坐标噪声和泊松杂波。

### 7.3 与其他场景的混合训练

`birth_death` 场景与已有的四类场景（星形交叉、多目标、高机动、纺锤形）进行等量混合，构成五类场景的均衡训练集：

$$
\mathcal{D}_\text{train} = \bigsqcup_{k=1}^{5} \mathcal{D}_k, \quad |\mathcal{D}_k| = \frac{N_\text{total}}{5}
$$

---

## 8. 推理时的航迹起始与终结判决

### 8.1 存在性阈值判决

在推理阶段，系统对每个轨迹槽位输出的存在性概率 $e_i$ 进行阈值判决：

$$
\hat{\xi}_i = \mathbf{1}\left[ e_i \geq \theta_\text{exist} \right]
$$

其中 $\theta_\text{exist}$ 为可配置的存在性阈值（默认值 0.5）。

- **航迹起始**：当目标首次进入视野，关联量测开始持续出现于对应槽位，$e_i$ 从低值逐步升高并超过 $\theta_\text{exist}$，系统宣告该航迹起始。
- **航迹终结**：当目标离开视野，对应槽位的历史状态中零填充比例增加，$e_i$ 逐步下降至 $\theta_\text{exist}$ 以下，系统宣告该航迹终结，并停止输出该槽位的位置估计。

### 8.2 输出过滤机制

滤波输出 $\hat{\mathbf{x}}_i$ 仅对满足 $\hat{\xi}_i = 1$ 的槽位有效：

$$
\text{Output} = \left\{ \hat{\mathbf{x}}_i \;\middle|\; e_i \geq \theta_\text{exist}, \; i = 1, \ldots, T_\text{max} \right\}
$$

对于 $e_i < \theta_\text{exist}$ 的槽位，其滤波输出不进入后续处理流程，避免"幽灵航迹"的持续输出。

---

## 9. 与传统算法的对比

| 维度 | 传统 MHT / JPDA | BAIT（本方案） |
|------|----------------|--------------|
| 航迹起始机制 | 基于确认帧数（M-of-N 逻辑） | 存在性概率端到端学习 |
| 航迹终结机制 | 连续漏检帧数阈值 | 存在性概率衰减自动触发 |
| 起始延迟 | 需 $M$ 帧确认（通常 3~5 帧） | 随量测积累连续概率上升，低延迟 |
| 对漏检的鲁棒性 | 单帧漏检可能误触发终结 | 历史注意力机制平滑估计，鲁棒性强 |
| 场景先验 | 无（规则固定） | 从训练数据中隐式学习，适应复杂场景 |
| 多目标生命周期交错 | 独立管理，无跨目标信息 | Cross-Bridge 使各槽位感知全局目标状态 |

---

## 10. 小结

本方案通过以下四项关键设计，将航迹起始与终结能力完整集成至 BAIT 框架：

1. **固定槽位一致性**：确保槽位与目标的绑定关系在全场景时间轴上稳定，支撑 Transformer 的跨帧注意力正确关联同一目标的历史信息。

2. **逐槽位 GT 存在性掩码 $\boldsymbol{\xi}$**：突破前缀连续性假设，精确表达任意生命周期组合的多目标场景，为存在性损失提供准确的监督信号。

3. **存在性感知损失函数**：联合优化关联精度、位置精度与生命周期判断，三项损失协同训练，避免仅优化单一目标导致的性能失衡。

4. **去真值化纯量测热启动**：消除训练与推理之间的信息不对称，使系统在真实部署条件下具有与训练场景一致的感知输入，保证泛化性能。
