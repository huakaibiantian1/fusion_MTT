# 多目标数据关联与轨迹预测方法汇报大纲

## 研究背景：传统多目标跟踪方法与问题

### 第 1 页：多目标跟踪任务定义

- 场景中同时存在多个运动目标，传感器每一帧输出一组带噪声的测量点。
- 核心任务包括目标起始、数据关联、状态估计、轨迹保持和目标终结。
- 难点来自杂波、漏检、目标交叉、目标出生死亡、测量噪声和目标数量变化。

建议配图：
- 一张二维或三维场景示意图：多个目标轨迹、当前帧测量点、杂波点。

### 第 2 页：传统滤波式多目标跟踪流程

- 常见流程是先预测每条已有轨迹，再根据门限或代价矩阵把测量分配给轨迹。
- 典型方法包括 KF/EKF/UKF + 最近邻、JPDA、MHT、PHD/CPHD 等。
- 这些方法依赖运动模型、噪声假设、门限参数和人工设计的轨迹管理规则。

建议配图：
- 流程图：预测 -> 门控 -> 关联 -> 更新 -> 起始/终结管理。

### 第 3 页：传统方法在复杂场景下的不足

- 目标交叉或距离接近时，单纯基于距离或似然的关联容易发生 ID switch。
- 杂波较多时，轨迹起始容易产生虚警航迹。
- 漏检或遮挡时，轨迹容易提前断裂。
- 固定参数难以同时适应不同噪声、不同密度和不同机动强度的场景。

建议配图：
- 交叉轨迹图：两条轨迹在交叉处发生身份交换。

### 第 4 页：为什么需要学习式数据关联

- 传统关联通常只利用当前预测和当前测量之间的局部关系。
- 学习式方法可以同时建模历史轨迹、当前测量集合、目标间关系和测量间关系。
- 模型可以从大量仿真样本中学习复杂关联规律，减少手工规则依赖。

建议配图：
- 对比图：传统方法只看局部距离，学习式方法同时看历史窗口和当前测量集合。

## 第一章：基于注意力机制的数据关联与预测方法

### 第 5 页：整体方法链路

- 输入包括历史轨迹窗口和当前帧测量集合。
- 历史轨迹窗口提供每条航迹的运动趋势和身份上下文。
- 当前测量集合提供本帧可能的目标观测。
- 模型输出轨迹-测量关联概率、目标状态预测和存在性概率。

建议配图：
- 模型结构图：历史轨迹窗口 + 当前测量点 -> 编码器 -> 跨注意力关联 -> 输出。

### 第 6 页：历史轨迹的自注意力建模

- 每条轨迹不是只用上一帧状态，而是使用一段历史窗口。
- 自注意力用于建模轨迹内部的时序运动模式，例如速度、加速度和机动趋势。
- 多条轨迹共同编码时，也可以表达目标之间的相对关系，帮助处理交叉和接近场景。

建议配图：
- 历史轨迹窗口示意：多帧状态点进入 self-attention，输出轨迹特征。

### 第 7 页：当前测量集合的自注意力建模

- 当前帧测量是一个无序集合，测量之间存在空间分布关系。
- 测量自注意力用于区分真实目标点、杂波点以及密集区域中的候选点。
- 该结构不依赖固定测量顺序，适合目标数量和测量数量变化的场景。

建议配图：
- 当前帧点云/测量集合图：真实测量点、杂波点，以及测量间注意力连接。

### 第 8 页：轨迹与测量之间的交叉注意力关联

- 交叉注意力让每条历史轨迹主动关注当前帧中最相关的测量。
- 关联不再只由欧氏距离决定，而是结合历史运动趋势、目标间上下文和测量集合结构。
- 输出可以形成轨迹-测量匹配矩阵，用于判断每条轨迹对应哪个测量，或是否未匹配。

建议配图：
- 关联矩阵热力图：行表示轨迹，列表示测量，颜色表示匹配概率。

### 第 9 页：状态预测与存在性估计

- 除了输出关联概率，模型还可以预测轨迹当前状态，提升漏检或噪声较大时的连续性。
- 存在性概率用于判断轨迹是否仍然有效，辅助航迹保持和终结。
- 数据关联、状态估计和存在性判断共同组成面向多目标跟踪的统一推理链路。

建议配图：
- 输出示意图：关联结果、预测轨迹点、存在性概率曲线。

### 第 10 页：相对传统方法的改进点

- 使用历史窗口，而不是只依赖上一帧状态，提升交叉和短时漏检场景下的稳定性。
- 使用集合建模，天然适应测量数量变化。
- 使用注意力机制融合轨迹上下文和测量上下文，减少单纯距离门控带来的误关联。
- 可以和传统航迹管理模块结合，由管理器负责起始/终结，由学习模型负责更强的数据关联和预测。

建议配图：
- 对比表：传统方法 vs 注意力式方法，在交叉、杂波、漏检、数量变化下的表现。

